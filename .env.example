# Copy to .env and fill in. Never commit .env.

# Exchange API (read-only keys are enough for historical data)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Local storage roots (optional overrides)
DATA_ROOT=./data
CATALOG_DB=./data/catalog.sqlite
