# INF-1 — Каркас проекта и тулинг

- **Эпик:** E0 — Scaffolding & инфраструктура
- **Статус:** done
- **Зависимости:** —

## Описание

Базовый каркас Python-проекта и инструментов разработки.

- `pyproject.toml` (менеджер пакетов: uv или poetry), пакет `src/trading_research/`.
- Линтеры/типизация: `ruff`, `mypy`.
- Тесты: `pytest` + структура `tests/`.
- `pre-commit` хуки (ruff + mypy + базовые проверки).
- `README.md`, `.env.example`.
- Создать пустые каталоги-плейсхолдеры по структуре из плана (раздел 11).

## Критерии приёмки (DoD)

- [ ] `pytest` запускается на пустом наборе тестов без ошибок.
- [ ] `ruff check` и `mypy` проходят на каркасе.
- [ ] Пакет `import trading_research` работает после установки в editable-режиме.
- [ ] `pre-commit run --all-files` проходит.
