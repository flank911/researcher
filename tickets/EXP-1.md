# EXP-1 — Grid/Random search

- **Эпик:** E3 — Experiment Engine
- **Статус:** done
- **Зависимости:** INF-2

## Описание

Генерация пространства параметров.

- `experiments/grid_search.py`: разворачивание `params_grid` в комбинации.
- `experiments/random_search.py`: случайная выборка с фиксированным seed.
- Детерминированный порядок комбинаций.

## Критерии приёмки (DoD)

- [x] Размер сетки и её содержимое детерминированы для заданного конфига.
- [x] Random search воспроизводим при том же seed.
- [x] Юнит-тесты на примере конфига из плана (раздел 12).
