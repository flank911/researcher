# EXP-3 — run_hash, dedupe, resume

- **Эпик:** E3 — Experiment Engine
- **Статус:** todo
- **Зависимости:** INF-3, EXP-2
- **Реализует улучшение:** №4 (детерминированный run_hash)

## Описание

Воспроизводимость и устойчивость экспериментов.

- `experiments/cache.py`: детерминированный `run_hash` через INF-3 от
  (strategy + params + symbol + timeframe + slice + dataset_hash + execution + git commit).
- Пропуск уже существующих ранов (dedupe).
- Resume после падения процесса.

## Критерии приёмки (DoD)

- [ ] Повторный запуск эксперимента не пересчитывает готовые раны.
- [ ] Убийство процесса и рестарт продолжают с места остановки.
- [ ] Изменение любого входа меняет `run_hash`; идентичные входы → тот же хэш.
